namespace Tests;

using CaptainCoder.Dungeoneering.DungeonMap;

using Shouldly;

public class DungeonEventMap_should
{
    public void trigger_on_enter_event()
    {
        /*
        MockAction action = ???;
        TileEvent event = new(EventTrigger.OnEnter, action);
        DungeonEventMap map = new();
        map.AddEvent(new Position(0, 1), event);
        
        */
    }
}